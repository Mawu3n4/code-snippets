

sounds = {
    '.': { 'vol': 100, 'len': 0.2},
    '-': { 'vol': 100, 'len': 0.6},
    '/': { 'vol': 0, 'len': 0.8},
    'freq': 440,
    'rate': 44100
    }

morse = [".-", "-...", "-.-.", "-..", ".", "..-.", "--.", "....", "..", ".---",
         "-.-", ".-..", "--", "-.", "---", ".--.", "--.-", ".-.", "...", "-",
         "..-", "...-", ".--", "-..-", "-.--", "--.", "-----", ".----", "..---",
         "...--", "....-", ".....", "-....", "--...", "---..", "----."]

u_input = raw_input('Enter a phrase to be coded: ')

print "/".join([ morse[ord(x) - ord('a')]
                 for x in u_input
                 if 'a' <= x <= 'z'
                 ])
